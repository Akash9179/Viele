# Viele — Project Memory

Living context & decision log for Viele. Complements `docs/SRS.md` (full vision), `docs/Viele-MVP-Plan.md` (the locked MVP slice), and `CLAUDE.md` (engineering rules). **Update this file whenever a decision is made or status changes.**

---

## Snapshot
- **Product:** personalized fashion discovery app — matches users to outfits, creators, and clothing by body type, proportions, complexion, and aesthetic. (Full vision in `docs/SRS.md`.)
- **Stage:** **Flutter app wired to Supabase — core loop is real (2026-06-18).** Auth (email), profiles, posting (Storage), feed (`feed()` RPC + match%), and all interactions (save/like/follow/block/report) + collections persist to the live `viele` project (ref `mdgublyyxcgpwvnmnlxe`). MVP scope confirmed by Eugene (2026-06-09); design locked (`docs/design.md`/`brand.md`). v1 = all posts public. GitHub: private `Akash9179/Viele`. **Readiness audit: `docs/readiness-audit.md`.** **Confirm-email is OFF** (dev). Test users in Auth: `viele-smoketest`, `viele-e2e-verify` (the latter authored ~10 seeded demo posts; app holds a persisted session for it).
- **Frontend status (2026-06-17):** app runs in the iOS sim (iPhone 17 Pro Max). **Built & reviewed with Akash, screen by screen:** onboarding (no-skip setup, height+weight required, "Not sure" removed), value-first funnel (onboarding → live feed; signup deferred to first save/post via `sessionProvider`+`requireAccount`), Feed, Outfit detail, **Discover** swipe deck (V2 pulled fwd), **Post compose** (redesigned: big cover preview, cover select, photo+video, tag items, separated caption/aesthetics, polished empty state), **Settings** (+ data export/delete, blocked accounts), **Moderation** (report/block — block hides content across feed/discover + Settings), **Profile depth** (Edit Profile page persists via `profileProvider`, Followers/Following `/connections`, Collections + detail). **`flutter analyze` clean.** See the dated log below for file paths.
- **Scope deltas to confirm with Eugene** (he owns product direction): Discover pulled into MVP frontend (was V2); Post **video** + on-photo **tag pins** added (spec said no-video / pins=V2); weight now **required** in onboarding (was optional). All flagged, none reverted.
- **People:** **Eugene** (**founder**; built the body-scan engine; owns product direction), **Akash Suryavanshi** (helping build the app — engineering/product execution).

## Status — as of 2026-06-09
- [x] PRD read & understood (`PRD.docx`)
- [x] SRS authored (`docs/SRS.md`) — full vision, prioritized P0/P1/P2
- [x] `CLAUDE.md` authored (engineering rules) — **hard rules reconciled to Eugene's 2026-06-09 confirmation** (scan→V2, attrs public, scan-privacy section scoped to V2)
- [x] `docs/memory.md` authored (this file)
- [x] CEO review of the SRS (`/plan-ceo-review`, SCOPE REDUCTION) → MVP scope locked
- [x] `docs/Viele-MVP-Plan.md` (+ PDF) authored for Eugene to confirm
- [x] **Eugene confirmed the MVP plan (2026-06-09)** — with changes: no AI scan, complexion in, any-user posting in
- [x] **Reconciled `docs/SRS.md` → v1.1** (2026-06-09): added a **Rel (MVP/V2)** column across all requirement tables, kept P0/P1/P2 priorities, reworded changed reqs ([CHG] tags), added new reqs (FR-ON.17 public-data disclosure, FR-CR.10 author-attr stamping, FR-RC.9/AI-17 declared-attribute matching, FR-SG.8 report/block), and rewrote §9 as the authoritative MVP-vs-V2 map. Resolved log in §10.4.
- [x] **Updated `docs/Viele-MVP-Plan.md`** to the confirmed scope (2026-06-09): reframed from "for Eugene's confirmation" → "Confirmed"; rewrote flow/features/matching/content/tech for self-reported onboarding, 3 tabs, UGC+moderation, declared-attribute matching, weight-private. **PDF NOT regenerated** (Akash's call) — `docs/Viele-MVP-Plan.pdf` is now stale vs the .md.
- [x] **Onboarding flow designed + APPROVED** (Akash, 2026-06-09) → spec at `docs/superpowers/specs/2026-06-09-onboarding-flow-design.md`. Memory: [[viele-onboarding-design]].
- [x] **Git repo initialized** (2026-06-09) — local, branch `main`, no remote yet; initial commit `38924e3`.
- [x] **SRS v1.1 → v1.2** (2026-06-15) — folded the approved onboarding-flow §5 deltas into `docs/SRS.md`: new FR-ON.18/.19/.20; amended FR-ON.3/.5/.8/.16/.17, NFR-9, ColorProfile, Appendix 11.3, §9.1.
- [x] **Post flow designed + APPROVED** (Akash, 2026-06-15) → spec at `docs/superpowers/specs/2026-06-15-post-flow-design.md`. Single-screen compose: gated entry (account / required-to-post mini-form / show-me-both silhouette) → photo carousel 1–8 + prefilled aesthetics + lightweight item list + Public/Followers/Private → publish immediately. Author stamp = publish-time snapshot (weight band server-side only). Post-publish = delete + edit-metadata (no media swap, no re-stamp).
- [x] **SRS v1.2 → v1.3** (2026-06-15) — folded the Post-flow §8 deltas into `docs/SRS.md`: reworked FR-CR.1/3/4/5/10, added FR-CR.11 (delete + edit-metadata) and FR-CR.12 (entry gates), updated the `Post` data entity + §4.4 purpose + §9.1.
- [x] **Feed flow designed + APPROVED** (Akash, 2026-06-15) → spec `docs/superpowers/specs/2026-06-15-feed-flow-design.md`. Driven by **Eugene's Home + Discover mockups** (shared 2026-06-15). Two-column masonry of match-cards w/ visible **match %**; chips For You/Most Similar/Your Aesthetic/Trending; recommended-people row; never-empty widening ladder; on-the-fly SQL serving; guest feed. **Scope call (Akash): mockups = visual north star, build the 3-tab subset (Feed/Post/Profile); Discover-swipe + Catwalk stay V2 — worth an Eugene confirm.**
- [x] **SRS v1.3 → v1.4** (2026-06-15) — folded the Feed-flow §10 deltas: reworked FR-HM.1/2/3/6/7/8, added FR-HM.16–19 (match-% display, filter chips, widening ladder, on-the-fly serving), annotated FR-RC.4 + AI-17, updated §9.1/§9.2 + headers. Note: FR-HM.2 "videos" → photo carousel (reconciled w/ FR-CR.1); FR-HM.7/.8 pulled to partial-MVP.
- [x] **Data architecture & schema designed + APPROVED** (Akash, 2026-06-15) → spec `docs/superpowers/specs/2026-06-15-data-architecture-design.md`. Tables (`profiles` public + owner-only `profiles_private` for weight/birthday; `posts` + `posts_private` weight-band; follows/likes/collections/collection_items/blocks; moderation_reports/admin_actions). RLS matrix. **Matching = `SECURITY DEFINER` `feed()`/`recommended_people()` Postgres RPCs** (read private weight, return public + match% only). **Media = single PRIVATE `post-media` bucket + signed URLs**, Storage read policy enforces visibility. Admin = `app_metadata.role='admin'`. Soft-remove moderation; cascade erasure w/ legal-hold carve-out. Realizes SRS §5 (no scope change).
- [x] **SRS v1.4 → v1.5** (2026-06-15) — folded schema concretizations: §5.1 split note, DR-4 table-split clarification, §3.4 feed() RPC + private media bucket rows, headers/footer. No new FRs.
- [ ] `docs/design.md` (reference: https://annafashion.lovable.app/)
- [ ] Scaffold Flutter app + React admin
- [x] **Provisioned the dedicated, isolated Supabase project** (2026-06-15, explicit go-ahead): name `viele`, **ref `mdgublyyxcgpwvnmnlxe`**, org `typtdvcjzflwpulemafi`, region `us-east-1`, free tier. **Only this project may be targeted.**
- [x] **Applied the schema as the first migration** (2026-06-15): `supabase/migrations/0001_initial_schema.sql` (11 tables, **RLS on every one**, profiles_private/posts_private isolation, helper fns, indexes, private `post-media` bucket) + `0002_security_hardening.sql` (citext→extensions, is_blocked→private schema). **Security advisor = 0 findings.** Migrations committed to repo under `supabase/`. Next: feed() RPC + auth providers + client wiring.
- [x] **App running in iOS simulator + first feedback pass started** (2026-06-16). GitHub remote added (private `Akash9179/Viele`). Onboarding feedback round 1 implemented (`app/lib/features/onboarding/`):
  - **No skip / no opt-out in the not-logged-in setup steps.** Removed the "Skip" affordance + "· optional" framing + "or skip" copy from silhouette / skin-tone / undertone; those steps now start unset and gate Continue on a real selection.
  - **Removed "Not sure"** options from `SilhouetteShape` and `Undertone` (enums + labels + icon painter) — every field now needs a concrete pick.
  - **Height + weight moved INTO the not-logged-in setup** as dedicated wheel steps (ft/in + lb), so matching data is captured pre-signup. Removed them from the post-account `_Finish` step (no longer asked twice). Progress bar 5 → 7 segments.
  - **Weight is now REQUIRED but still PRIVATE** — overrides the earlier "weight optional" decision (Akash's call). UI keeps the lock badge "Private — matching only. Never shown on your profile." `CLAUDE.md` privacy rule updated; `docs/Viele-MVP-Plan.md` still says "weight (optional)" in a few places → **reconcile when next editing the plan.** SRS deltas pending.
  - Verified live on the iPhone 17 Pro Max simulator (silhouette / height / weight screens). `flutter analyze` clean.
- [x] **Onboarding feedback round 2 — value-first funnel realigned** (2026-06-16). Akash didn't understand the "wow" preview screen; root cause = it duplicated the real feed AND its "no account needed" CTA led straight into a forced signup wall, contradicting the locked value-first design. **Decision: cut the wow screen; drop straight into the live feed after onboarding; defer signup to the first save/post.**
  - Removed the `_Wow` step. Onboarding now ends at the undertone step → `context.go('/home')` (the real feed). No forced account/basics/finish.
  - Added in-memory **`sessionProvider`** (`lib/core/state/session.dart`) — guest by default; `signIn()` flips it. Plus a **`requireAccount(context, ref, action)`** gate helper that runs the action if signed in, else pushes `/signup` and runs it only if they sign in (pending save/post completes after signup, dropped if cancelled).
  - The Account→Basics→Finish screens became a reusable **`SignupFlow`** at route **`/signup`** (pushed full-screen, with a cancel ✕). Choosing a provider flips `signedIn`.
  - **Gated actions = save (feed card + outfit detail) and the Post tab** (`app_shell` → ConsumerWidget). Like/follow left ungated for now (follow-up). "I already have an account" on Welcome signs in + goes home.
  - Verified rendering live (feed `/home`, signup `/signup` with ✕). `flutter analyze` clean. Couldn't auto-drive sim taps, so the gate interaction is verified by code review, not a live tap.
- [x] **Discover swipe deck built (V2 surface pulled forward)** (2026-06-16, Akash's pick). Replaced the `PlaceholderScreen` stub with a real swipe deck: `app/lib/features/discover/presentation/discover_screen.dart`. Custom gesture mechanics (no new package): drag/fling top card with rotation + LIKE/PASS stamps, peek of next card behind, endless by looping `mockFeed`, action bar (Pass ✕ / Like ♥), "why this matches" insight chip (FR-DS.5), match pill, account-gated bookmark save. **Swipe-right = like (ungated, in-memory training signal); bookmark = gated save** — consistent with the save/post gate. **Scope note:** Discover is V2 in the locked plan (`design.md` §9, EIR-2 3-tab MVP); this is the *frontend* pulled forward — preference-learning/recommendation backend (FR-DS.2) stays V2. `flutter analyze` clean; rendering verified in sim (swipe gesture not auto-testable, verified by code review).
- [x] **Settings page built (+ data export & account deletion)** (2026-06-17, Akash "Next"). New full-screen `SettingsScreen` at route `/settings` (`app/lib/features/profile/presentation/settings_screen.dart`); replaced the dead non-working settings bottom-sheet in profile (gear icon now `context.push('/settings')`). Grouped iOS-style sections: Account (email, change password), Privacy & data (**Blocked accounts** sub-page w/ unblock, **Export my data**, privacy policy), Notifications (CupertinoSwitch toggles), About, Sign out (→ signOut + `/onboarding`), **Delete account** (destructive confirm sheet → signOut). Export + Delete use `_ConfirmSheet` with real copy; frontend-mocked (Supabase wiring later). Satisfies the MVP **data export + deletion** privacy requirement (frontend). `flutter analyze` clean; rendered in sim (taps/sheets verified by code review).
- [x] **Post compose redesigned** (2026-06-17, Akash feedback). `post_compose_screen.dart` → StatefulWidget. Big **cover preview** (4:5, "how the look appears") replacing tiny thumbs; **cover selection** via thumbnail strip (tap to set cover + remove ×); **Add Photo / Add Video** tiles; on-photo **tag pins + "Tag items"** pill → add-item sheet (name/brand/link); **Caption** and **Aesthetics** now each their own separated `_SectionCard` (were flat/buried); Items + Visibility(Public v1) also carded. **Two deltas from the approved post-flow spec (Eugene owns product direction): (1) VIDEO — spec says "no video at MVP"; (2) on-photo TAG PINS — spec says image pins are V2 (MVP = name/brand/link rows only).** Both added per Akash's request → flag to Eugene. `flutter analyze` clean; rendered in sim. **Polished empty state** (2026-06-17): `_emptyHero` — soft sand icon badge, "Add your first look" + value copy, Add photo (ink) / Add video (outline) buttons, framing tip; Share disabled until media added. Debug defines added for screenshots: `SCROLL` (initial list offset), `EMPTY` (start with no media) — both default off.
- [x] **Moderation (report/block) built** (2026-06-17, Akash's pick — MVP requirement, `docs/moderation.md`). New shared module `app/lib/features/moderation/presentation/moderation_actions.dart`: `showReportSheet` (7 policy reasons — sexual/harassment/violence/illegal/spam/IP/other → anonymous "reviewed within 24h" confirmation) + `confirmBlock` (warning sheet → applies block + snackbar). Wired the dead overflow rows in **outfit detail** (`_openOverflow(context, ref, author)`) and added an overflow ⋯ to **other-user profile** (Report @handle / Block name). **Block actually hides content:** added `blocked` set + `block`/`unblock` to `interactionsProvider`; **feed** + **discover** filter blocked authors; blocking pops the author's post/profile; **Settings → Blocked accounts** now reads the same set with working Unblock (replaced the static demo list). Debug define `SHEET=report|block` (in outfit_detail) auto-opens a sheet for screenshots (400ms delay so the overlay is ready), default off. `flutter analyze` clean; report + block sheets verified live in sim.
- [x] **Profile depth — COMPLETE** (2026-06-17, Akash's pick). Part 1 + Collections.
  - **Collections** (part 2): new `collectionsProvider` (`lib/core/state/collections.dart`, mock named collections + `create`). Profile **Saved tab → collections grid** (cards: "All saved" virtual from saved set + named collections + "New collection" sheet). New **Collection detail** page `/collection` (`collection_detail_screen.dart`, extra = `({name, posts})`, masonry grid → `/outfit`). Debug `--dart-define=TAB=1` opens profile on Saved.
  Part 1 details follow:
- [x] **Search built** (2026-06-17, Akash's pick — last page group). New `SearchScreen` route `/search` (`app/lib/features/search/presentation/search_screen.dart`); wired the feed header search icon (made `_CircleIcon` tappable). Autofocus search bar; **empty query = discovery** (browse-aesthetics chips + people-to-discover list); **typing = sectioned results** (Aesthetics pills / People rows w/ Follow / Looks 3-col grid) with no-results empty state. Blocked authors excluded. People → `/user`, looks → `/outfit`, aesthetic tap refills query. Debug `--dart-define=Q=...` prefills query. `flutter analyze` clean; both states verified in sim. **→ Frontend page groups now ALL done (Discover, Settings, Moderation, Profile depth, Search). Next phase: wire Supabase (auth + feed() RPC + Storage) replacing the mock providers; React admin console still not started.** (a) New **`profileProvider`** (`lib/core/state/profile.dart`) holding the user's editable profile (name/username/bio/region/aesthetics/height/shape/hair/eye/avatar; weight excluded — private). (b) Full **Edit Profile page** `/edit-profile` (`edit_profile_screen.dart`) — replaced the old sheet; editable text fields + attribute rows with Public/Private badges + pickers (height wheel, shape/hair/eyes option sheets reusing onboarding_data); Save persists to provider. (c) **Connections page** `/connections` (`connections_screen.dart`) — Followers/Following tabs, people from feed authors, Follow toggles shared state, tap → `/user`. Profile tab now reads the provider; stats Followers/Following are tappable; Edit button → `/edit-profile`; removed dead `_openEditProfile` sheet. **Still TODO for Profile depth: Collections / saved detail.** `flutter analyze` clean; both pages verified in sim.

## The MVP (current build target)
Locked via CEO review, **confirmed + amended by Eugene 2026-06-09.** `docs/Viele-MVP-Plan.md` still needs updating to match.

**Wedge:** open the app and within ~1 minute see real outfits worn by people built like you, in your aesthetic, and save them.

- **Three tabs only: Feed, Post, Profile.** No Discover, no Catwalk.
- **Open to everyone**, all body types/genders/aesthetics, day one. (Audience narrowing was explicitly rejected.)
- **No AI body/face scan at MVP.** Onboarding is self-reported form entry: height, weight, body silhouette, + hair/skin/eye color (simple categorical, e.g. brown/black/blonde). AI scan = optional V2 accuracy upgrade.
- **Complexion IS in MVP** (was deferred) — simple self-reported categorical, not AI detection.
- **Matching = declared attributes on both sides.** Auto-tagging keys off the poster's onboarding info (height/weight/silhouette/coloring); posters self-tag those same fields. The pose-pipeline-on-content shape-ratio idea moves to V2 with the scan.
- **Attributes are public**, shared via posts (height, silhouette, coloring). **Weight is the carve-out:** optional, **private/matching-only, never displayed** (resolved 2026-06-09; deviates slightly from Eugene's "all public" — confirm with Eugene).
- **Any user can post (UGC) at MVP** — the Post tab. Reverses the earlier creator-roster-only / no-UGC decision → **re-adds a moderation need** (see open decisions).
- **Deferred to later phases (still in SRS):** Discover swipe, Catwalk, AI body/face scan + complexion detection, pose-pipeline shape-ratio matching, full social graph, full admin, recommendation sophistication, brands/budget/goals.

## Decision log
| Date | Decision | Rationale |
|---|---|---|
| 2026-06-08 | Mobile app = **Flutter** (iOS + Android) | User prioritized UI polish; accepted rewriting the body-scan engine over React Native's reuse advantage |
| 2026-06-08 | Separate **React web** super-admin dashboard | Web admin tooling kept distinct from the mobile product |
| 2026-06-08 | Backend = **Supabase** | pgvector for recommendation embeddings in the same Postgres; unifies app + admin; Auth matches onboarding; RLS for sensitive data |
| 2026-06-08 | **Dedicated, isolated** Supabase project | User runs another project on the same account — zero cross-contamination required (hard rule) |
| 2026-06-08 | Body scan **ported to Dart**, algorithm preserved | Eugene's measurement math is DOM-independent; only the capture layer is rebuilt natively |
| 2026-06-08 | **Client-side-first** AI; raw imagery never uploaded | Privacy by design for body/face data |
| 2026-06-08 | SRS scope = **full vision, phased P0/P1/P2** | Complete spec with a realistic build order |
| 2026-06-08 | Monetization = **P2 + open question** | No business model defined yet |
| 2026-06-08 | **CEO review → SCOPE REDUCTION → sharp wedge MVP** | Market research: these apps die from onboarding friction + content cold-start; build the core loop first |
| 2026-06-08 | **Open to everyone** (no segment narrowing) | Product call: anyone can install and get a useful feed day one |
| 2026-06-08 | Body scan placed **value-first** (after the wow) | Defuses the camera-first onboarding wall (25-60% abandonment) while keeping the moat |
| 2026-06-08 | **Cold-start = auto-tag content with the pose pipeline** + graceful widening | Reuses the moat tech to body-tag the catalog by shape ratios; no audience restriction needed |
| 2026-06-08 | Content via **recruited body-diverse creators**; no UGC at MVP | Real body diversity needs real people; dropping UGC removes moderation burden — *superseded 2026-06-09 ↓* |
| 2026-06-09 | **Eugene confirmed MVP scope** with amendments (below) | Partner sign-off on the build target |
| 2026-06-09 | **No AI body/face scan at MVP**; self-reported onboarding instead (height, weight, silhouette, coloring) | Eugene: scan is V2-optional; avoids camera-first abandonment, big scope cut |
| 2026-06-09 | **Complexion back IN at MVP** — simple self-reported categorical (hair/skin/eye) | Eugene wants coloring in the match; kept simple (dropdowns), not AI detection |
| 2026-06-09 | **Matching = declared attributes** both sides; pose-pipeline shape-ratio matching → V2 | Follows from dropping the scan; simpler & more transparent (Akash accepted) |
| 2026-06-09 | **Attributes public** (height/silhouette/coloring) | Eugene: shared via posts. No raw imagery exists → low risk |
| 2026-06-09 | **Weight = private, matching-only, never displayed** | Akash (OQ-18): most sensitive field; used as a coarse match band, not shown. Slight deviation from Eugene's "all public" — confirm with Eugene |
| 2026-06-09 | **Any user can post (UGC)** at MVP — Post tab; reverses no-UGC | Eugene's 3-tab spec (Feed/Post/Profile); Akash: "any users can post for now" → moderation now needed |
| 2026-06-09 | **Tabs = Feed, Post, Profile** only | Eugene: no Discover, no Catwalk at MVP |
| 2026-06-09 | **Moderation = post-moderation; founders review ≤24h; no auto-screen at MVP** | Akash's calls; scale-appropriate + meets Apple 1.2 / Play UGC floor. Full policy in `docs/moderation.md` |
| 2026-06-09 | **Onboarding = 3-stage value-first** (anon teaser → account at first save/post → gentle Stage-3) | Brainstorm; camera gone so value-before-account is the strongest wedge. Spec on disk; see `[[viele-onboarding-design]]` |
| 2026-06-09 | Onboarding specifics: **Monk-10 skin tone; req-to-post = height+hair+eye; body-type-set ≠ gender identity; layered public disclosure** | Inclusive + low-friction + UGC-stamping needs; Akash's calls in brainstorm |
| 2026-06-15 | **Design system direction locked** — Eugene's Home layout, **native SF Pro (San Francisco) type** (no decorative serif), light warm-cream canvas, green match accent, real photography, iOS app chrome. Mockups in `~/.gstack/projects/Viele/designs/`. See [[akash-design-taste]]. | Akash: must look like a real Apple app, not generic AI/template; founder layout kept |
| 2026-06-15 | **v1 = ALL POSTS PUBLIC; drop Followers/Private → V2** | Eugene. Simplifies RLS (no follower-scoped reads), simpler media storage, maximizes cold-start feed content. **Pending spec folds:** SRS FR-CR.5, Feed-flow §5 access, data-architecture RLS/storage |
| 2026-06-15 | **Onboarding sensitivity + taxonomy refinements** (design): shape/skin-tone screens made optional + reassuring; **height = iOS wheel picker**; **hair/eye taxonomies expanded** (hair: +Dark brown/Light brown/Auburn/Platinum/Colored; eyes: +Dark brown/Amber). Folded into SRS App 11.3. | Akash: shape/coloring selection is sensitive — make it kind + not offensive |
| 2026-06-15 | **Undertone pulled into MVP** (was V2): Warm/Cool/Neutral/Not sure step after skin tone. Plus skin-tone selection accuracy aids (jaw/wrist tip, bigger swatches, confirm strip). | Akash (trust): self-selected skin tone must be accurate or matches feel random. Monk-10 confirmed industry standard (Google-adopted, > Fitzpatrick); undertone is the key 2nd accuracy axis; selfie auto-detect = V2. Updated SRS FR-ON.8/RC.3/App11.3/ColorProfile, design.md §7 |
| 2026-06-15 | **Body-shape labels = recognizable fashion-industry standard names + descriptor** (Hourglass/Pear/Rectangle/Apple-Round/Inverted Triangle/Not sure), NOT invented neutral labels. Kept refined **line-art croquis** silhouettes (CustomPainter; replaced crude filled blobs). | Research-backed (Akash asked): industry standard ("pear" etc.) drives self-identification; kindness via framing (optional, non-prescriptive — match not "fix"), not via avoiding the words. Updated SRS App 11.3, design.md §7, brand.md §8 |

## Open decisions / gates
- ~~GATE — Eugene to confirm MVP plan Section 9~~ **RESOLVED 2026-06-09** (see decision log). The pose-pipeline cold-start is moot for MVP — matching is now on declared attributes, scan→V2.
- ~~NEW — moderation for UGC~~ **RESOLVED 2026-06-09 → `docs/moderation.md`.** Post-moderation (publish immediately), founders (Eugene/Akash) review reports ≤24h, no automated pre-screen at MVP, mandatory zero-tolerance EULA, report+block ship at MVP. Follow-ups: EULA/Guidelines copy, abuse-contact address, CSAM/NCMEC runbook.
- ~~"Post" tab semantics~~ **RESOLVED 2026-06-09:** "Post" = **users posting their own outfits** (true UGC), confirmed by Eugene via Akash.
- Longer-term (SRS §10): onboarding steps, budget/fashion-goals capture, age gating, monetization (P2), admin framework (React SPA vs Next.js).

## Environment notes
- gstack upgraded to **1.57.6.0** (2026-06-08).
- `make-pdf` / `browse` need `bun`, which is at `~/.bun/bin/bun` but not on the non-interactive shell PATH. Prepend `export PATH="$HOME/.bun/bin:$PATH"` before running. gstack network/upgrade commands need the sandbox disabled.

## Next steps
1. ~~Reconcile `docs/SRS.md`~~ **DONE (v1.1).**
2. ~~Update `docs/Viele-MVP-Plan.md`~~ **DONE** (text only; PDF intentionally stale).
3. ~~MVP **moderation** policy~~ **DONE → `docs/moderation.md`.**
4. ~~Weight-display default (OQ-18)~~ **DONE** — private/matching-only.
5. ~~Onboarding spec~~ **APPROVED.** ~~git init~~ **DONE** (`main`, commit `38924e3`).
6. ~~Fold onboarding §5 deltas into `docs/SRS.md` (v1.1→v1.2)~~ **DONE 2026-06-15.** Added FR-ON.18 (value-first 3-stage staging), FR-ON.19 (body-type set, decoupled from gender), FR-ON.20 (required-to-post = height+hair+eye); amended FR-ON.3/.5/.8 (Monk-10 ordinal)/.16 (guest=teaser, now MVP)/.17 (layered disclosure), NFR-9, ColorProfile data model, Appendix 11.3, §9.1, headers. Committed `cff1d89`.
7. ~~Finalize the Post flow~~ **DONE 2026-06-15** — spec `docs/superpowers/specs/2026-06-15-post-flow-design.md` (committed `1fa5e33`), folded into SRS v1.3 (FR-CR.11/.12 added, FR-CR.1/3/4/5/10 reworked, Post entity).
8. ~~Brainstorm the Feed flow~~ **DONE 2026-06-15** — spec `docs/superpowers/specs/2026-06-15-feed-flow-design.md` (committed `b47cd65`), folded into SRS v1.4 (FR-HM.16–19 + reworks). Driven by Eugene's mockups; 3-tab subset.
9. ~~start building the app~~ **DONE** — Flutter frontend largely built (see Snapshot + dated log). Working norm with Akash: **one screen/feature at a time, build → run in sim → screenshot → review.**

**📋 READINESS AUDIT (2026-06-18):** full multi-agent audit of real-user readiness → **`docs/readiness-audit.md`** (29 P0 / ~24 P1 / ~18 P2). Verdict: front-end prototype on mock data; DB security strong; core loop not wired. The doc's "minimal critical path" is the agreed build order from here.

**← RESUME HERE (2026-06-18, end of day):** Supabase wiring slices **1–8 DONE + verified** (auth → profiles → identity/UUID refactor → posting → feed → interactions persist → collections persist → profile real). Each is logged in detail below with file paths + live-REST verification.
   **Next (finish wiring read surfaces so nothing shows mock):** Discover deck, Search (people/looks), the feed Recommended row, **other-user profile** (still hardcoded attrs), and outfit-detail "shop the look" items — all still read `mockFeed`/`mockRecommended`. Plan: a `people()`-style query + reuse `feedPostFromPostRow`/`feed()` mapping.
   **Then (pre-tester):** body-diverse **multi-author seed** (needs service_role to create posters — current demo seed is one author); **legal pages** (privacy/guidelines wired to real URLs) + working account delete/export (Edge Function); **branded app icon + splash** (still default Flutter); **auth routing redirect** (signed-in user still lands on /onboarding); OAuth (Google/Apple, needs dashboard+native); React **admin/moderation console** (separate app, not started).
   **Debug defines** (screenshots, all off by default): `ROUTE`,`START`,`SCROLL`,`EMPTY`,`SHEET`,`TAB`,`Q`,`AUTH`,`DEVLOGIN=email|password` (auto sign-in).

(Historical resume note — superseded by the above:)
   - **Slice 1 DONE — client + email auth.** Added `supabase_flutter 2.15.0` (pinned, lockfile committed). `lib/core/supabase/supabase_config.dart` (URL + **publishable** key — committed, client-safe). `main.dart` → `Supabase.initialize(url, publishableKey:)`; logs "Supabase init completed" against live project. `sessionProvider` now backed by **real Supabase auth** (onAuthStateChange; `signUpWithEmail`/`signInWithEmail`/`signOut`). New `email_auth_sheet.dart` (`showEmailAuth`) wired into SignupFlow's "Sign up with email" + Welcome "I already have an account". Apple/Google buttons → "coming soon" snackbar (need provider config). Debug `--dart-define=AUTH=email` opens the sheet. Verified: live signup endpoint works with publishable key (curl smoke test).
   - **⚠️ TWO follow-ups for Akash:** (1) **"Confirm email" is ON** in viele Auth settings → email signup needs confirmation before sign-in (code shows "check your email"). For frictionless dev, disable it in the dashboard (Auth → Providers/Email) — couldn't toggle via available MCP tools. (2) **Leftover smoke-test user** `viele-smoketest@gmail.com` in `auth.users` — my DELETE was blocked (live-DB safety); remove via dashboard or grant permission.
   - **Slice 2 DONE + VERIFIED — Profiles (2026-06-18).** Akash **disabled Confirm-email** in the dashboard (signups now return a session). New `onboardingDraftProvider` (snapshots teaser answers at onboarding finish) + `ProfileRepository` (`core/data/profile_repository.dart`): on signup, creates `profiles` (+ owner-only `profiles_private.weight_kg`) from draft + collected username/name/region/hair/eye. SignupFlow `_Basics` now real editable fields (username required, 23505 "taken" handled); `_complete` async-creates the profile (skips if exists). Mapping: ft/in→`height_cm`, lb→`weight_kg`, Monk idx→`skin_tone` 1-10, silhouette→`inverted_triangle`, enum `.name` for body_type_set/undertone. **Verified live via REST**: owner insert 201, owner-only weight upsert 201, read-back OK, and **RLS rejects a foreign-id insert (403)**. ⚠️ Leftover test users to delete in dashboard: `viele-smoketest@gmail.com`, `viele-e2e-verify@gmail.com` (classifier blocks me deleting auth.users). `flutter analyze` clean.
   - **Slice 3 DONE — Identity refactor name→UUID (2026-06-18).** `FeedPost` now has `authorId` (mock ids u1–u6); `interactions` follow/block keyed by **authorId** (not name); `/user` route extra carries `id`; `confirmBlock(userId, name)`; recommended row + connections + search people carry ids; feed/discover/search blocked-filters use `authorId`; Settings blocked list resolves id→name for display, unblocks by id. Client-only, no DB change; no visual regression (verified). `flutter analyze` clean. Prerequisite for interaction persistence + safe blocking is now satisfied.
   - **Slice 4 DONE + VERIFIED — Posting (2026-06-18).** Added `image_picker 1.2.2` + `uuid 4.5.3` (pinned). iOS Info.plist: `NSPhotoLibraryUsageDescription`, `NSCameraUsageDescription`, `ITSAppUsesNonExemptEncryption=false`. New `PostRepository` (`core/data/post_repository.dart`): uploads media to `post-media/posts/{uid}/{postId}/{n}.jpg` (matches RLS `foldername[2]=auth.uid()`), inserts `posts` (caption/aesthetics/items/media/author_snapshot/visibility) + `posts_private` weight band (band=floor(kg/5)). Post compose rewritten: real gallery+camera picker, local-file previews (Image.file), **video removed** (MVP scope), selectable aesthetics (≥1 required), cover-first ordering, **redundant height/hair/eye gate removed** — Share now publishes directly (loading state, validation, error toast). **Verified live via REST**: owner upload 200, **storage RLS rejects foreign path 403**, posts insert 201, posts_private insert 201 (return=minimal, as the app does). ⚠️ Test artifacts in DB (post `0117a591…`, a storage object, weight band) + test users `viele-smoketest`/`viele-e2e-verify` — delete in dashboard. NOTE: rendering uploaded media needs Step 5 (bucket is private → make public-read OR signed URLs). `flutter analyze` clean.
   - **Slice 5 DONE + VERIFIED — Real feed (2026-06-18).** Migrations `0003_feed` (feed() RPC + `post-media` bucket → public), `0004_storage_public_hardening` (dropped broad bucket SELECT/list policy), `0005_feed_definer` (feed() → **SECURITY DEFINER**, search_path='', returns only public posts+author public fields+match%, enforces visibility+block-pairs, **grant anon+authenticated** so GUESTS can browse). Match% = silhouette(30)+height-band(20)+skin-tone(15)+undertone(10)+aesthetic-overlap(25) vs viewer profile. Client: `feed_repository.dart` (`feedProvider` FutureProvider; maps RPC→FeedPost, builds public media URLs, cm→ft/in, initials); `feed_screen` → AsyncValue.when with **loading/error+retry/empty states + pull-to-refresh**; post publish invalidates feedProvider. **Verified live**: feed() returns rows for authed (match 100) AND anon/guest (match 8); RLS still blocks direct table reads. Seeded **6 real-photo posts** (single author 'E2E Verify') for visual demo — feed renders real images in-app. `flutter analyze` clean.
   - **Accepted advisor WARNs:** feed() is anon/authenticated-executable SECURITY DEFINER **by design** (public feed RPC, returns only public data, weight never exposed); leaked-password-protection OFF (P2 dashboard toggle).
   - **Slice 6 DONE + VERIFIED — Interactions + report persistence (2026-06-18).** `interactionsProvider` rewritten: loads likes/follows/blocks from DB on launch (signed-in); **optimistic writes with revert-on-error** for `toggleLike`→likes, `toggleFollow`→follows, `block`/`unblock`→blocks. Guests get in-memory-only (funnel nudges signup at save/post). No call-site changes (signatures unchanged). `showReportSheet` now takes `postId` and **inserts `moderation_reports`** (label→reason-code map) for post reports → founder queue; user-level reports show confirmation only (schema has no reported_user; post_id NOT NULL). Threaded post.id through outfit-detail overflow. **Verified live**: like insert 201 + feed likes count reflects it; report insert 201 (reporter reads own, status 'open'); load queries 200. Follow/block use identical pattern (couldn't fully exercise — only 1 profile exists). Blocks now persist → feed() server-side exclusion is real. **Saves still in-memory** (collections wiring is next). `flutter analyze` clean.
   - **Slice 7 DONE + VERIFIED — Saves + collections persistence (2026-06-18).** Bookmark = membership in a per-user default **'Saved'** collection. `interactions`: loads saved post_ids on launch + `toggleSave` persists (collection_items insert/delete, optimistic+revert, default-collection get-or-create cached). New `collections_repository.dart`: `namedCollectionsProvider` (collections excl. 'Saved' + count + cover), `savedPostsProvider` (postsByIds of saved set → FeedPost via author_snapshot), `collectionPostsProvider(id)`, `createCollection`. Profile Saved tab → real ("All saved" + named cards + New); `collection_detail_screen` → ConsumerWidget watching provider by collectionId (or saved), AsyncValue.when. `/collection` route extra = `({name, collectionId?})`. Deleted mock `core/state/collections.dart`. **Verified live**: save insert 201, named-collection create 201 + listed, collection-detail post fetch OK, **RLS rejects foreign-owner collection 403**. `flutter analyze` clean. (Riverpod: use `.asData?.value`, not `valueOrNull`.)
   - **ALL interactions now persist** (save/like/follow/block/report). 
   - **Slice 8 DONE + VERIFIED — Profile → real data (2026-06-18).** `profileProvider` loads the signed-in user's `profiles` row on launch + `save()` updates it (PostgrestException 23505→"username taken"); maps body_silhouette↔label, height_cm↔ft/in, aesthetics[]↔"· " string. `profile_repository`: `myPostsProvider` (own posts → FeedPost) + `followCountsProvider` (real counts). Shared `feedPostFromPostRow` mapper added to `feed_repository` (used by profile + collections; removed the dup). Profile screen: real identity/avatar, **real Posts grid** (empty state if none), **real follower/following counts**, posts count. EditProfile save → DB + error toast + saving state. **Verified live (DEVLOGIN hook)**: signed-in profile shows real name/@handle/aesthetics/attribute-chips/10-posts grid/0-0 counts; screenshot confirms. Added debug `--dart-define=DEVLOGIN=email|password` in main.dart (auto sign-in for screenshots; no creds committed). NOTE: app now has a **persisted test session** (viele-e2e-verify) from the dev login — sign out via Settings or it stays signed in. `flutter analyze` clean.
   - **Slices remaining:** ~~Profiles, Feed, Interactions, Posting~~ **ALL DONE (slices 1–8).** See the current "RESUME HERE" pointer near the top of this Status section for what's next.
   - **App conventions (for fast iteration):** debug `--dart-define` hooks exist for screenshots — `ROUTE=/path` (initial route), `START=n` (onboarding step), `SCROLL=n` + `EMPTY=true` (post compose), `SHEET=report|block` (outfit detail), `TAB=1` (profile Saved). To screenshot the running sim: `xcrun simctl io <booted-udid> screenshot /tmp/x.png`. Can't auto-drive taps/drags — verify gesture flows by code + relaunch-with-define.
10. Pre-launch follow-ups: confirm scope deltas + weight-private with Eugene; **confirm 3-tab-subset vs Discover-in-MVP**; EULA/Community Guidelines copy; abuse-contact; CSAM/NCMEC runbook; initial poster/creator seed plan.
10. Pre-launch follow-ups: confirm weight-private with Eugene; **confirm 3-tab-subset MVP with Eugene** (mockups show full 5-tab vision); EULA/Community Guidelines copy; abuse-contact; CSAM/NCMEC runbook; initial poster/creator seed plan.
